---
title: 從 ASP.NET Core 2.2 遷移至3。0
author: rick-anderson
description: 瞭解如何將 ASP.NET Core 2.2 專案遷移至 ASP.NET Core 3.0。
ms.author: riande
ms.custom: mvc
ms.date: 05/03/2020
no-loc:
- ASP.NET Core Identity
- cookie
- Cookie
- Blazor
- Blazor Server
- Blazor WebAssembly
- Identity
- Let's Encrypt
- Razor
- SignalR
uid: migration/22-to-30
ms.openlocfilehash: c76eff595d0be1eebaac00bf0292a6dbca2649db
ms.sourcegitcommit: d1a897ebd89daa05170ac448e4831d327f6b21a8
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/01/2020
ms.locfileid: "91606818"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a>從 ASP.NET Core 2.2 遷移至3。0

[Scott Addie](https://github.com/scottaddie)與[Rick Anderson](https://twitter.com/RickAndMSFT)

本文說明如何將現有的 ASP.NET Core 2.2 專案更新為 ASP.NET Core 3.0。 建立新的 ASP.NET Core 3.0 專案時，可能會有説明：

* 與 ASP.NET Core 2.2 程式碼比較。
* 將相關的變更複製到您的 ASP.NET Core 3.0 專案。

## <a name="prerequisites"></a>必要條件

# <a name="visual-studio"></a>[Visual Studio](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-code"></a>[Visual Studio Code](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mac"></a>[Visual Studio for Mac](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-net-core-sdk-version-in-globaljson"></a>更新 global.json 中的 .NET Core SDK 版本

如果您的解決方案依賴檔案 [ 上的global.js](/dotnet/core/tools/global-json) ，以特定的 .NET Core SDK 版本為目標，請將其 `version` 屬性更新為安裝在您電腦上的3.0 版：

```json
{
  "sdk": {
    "version": "3.0.100"
  }
}
```

## <a name="update-the-project-file"></a>更新專案檔

### <a name="update-the-target-framework"></a>更新目標 Framework

ASP.NET Core 3.0 和更新版本只會在 .NET Core 上執行。 將 [目標 Framework 標記 (TFM) ](/dotnet/standard/frameworks) 設定為 `netcoreapp3.0` ：

[!code-xml[](22-to-30/samples/Web1.csproj?highlight=4)]

### <a name="remove-obsolete-package-references"></a>移除淘汰的套件參考

ASP.NET Core 3.0 不會產生大量的 NuGet 套件。 您應該從專案檔中移除這類套件參考。 針對 ASP.NET Core 2.2 web 應用程式，請考慮下列專案檔：

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netcoreapp2.2</TargetFramework>
    <AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.App"/>
    <PackageReference Include="Microsoft.AspNetCore.Razor.Design" Version="2.2.0" PrivateAssets="All" />
  </ItemGroup>

</Project>
```

ASP.NET Core 3.0 的已更新專案檔：

[!code-xml[](22-to-30/samples/Web1.csproj?highlight=4)]

更新的 ASP.NET Core 3.0 專案檔：

* 在 `<PropertyGroup>` 中：
  * 將 TFM 更新為 `netcoreapp3.0`
  * 移除專案 `<AspNetCoreHostingModel>` 。 如需詳細資訊，請參閱本檔中的同 [進程裝載模型](#in-process-hosting-model) 。

* 在 `<ItemGroup>` 中：
  * `Microsoft.AspNetCore.App` 已移除。 如需詳細資訊，請參閱本檔中的 [架構參考](#framework-reference) 。
  * `Microsoft.AspNetCore.Razor.Design` 已移除，且已在下列套件清單中不再產生。

若要查看不再產生之套件的完整清單，請選取下列展開清單：

<details>
    <summary>按一下以展開不再產生的套件清單</summary>
    <ul>
        <li>Microsoft.AspNetCore</li>
        <li>Microsoft.AspNetCore.All</li>
        <li>Microsoft.AspNetCore.App</li>
        <li>Microsoft.AspNetCore.Antiforgery</li>
        <li>Microsoft.AspNetCore.Authentication</li>
        <li>Microsoft.AspNetCore.Authentication.Abstractions</li>
        <li>AspNetCore 驗證。 Cookie！</li>
        <li>Microsoft.AspNetCore.Authentication.Core</li>
        <li>Microsoft.AspNetCore.Authentication.OAuth</li>
        <li>Microsoft.AspNetCore.Authorization.Policy</li>
        <li>AspNetCore。 Cookie政策</li>
        <li>Microsoft.AspNetCore.Cors</li>
        <li>Microsoft.AspNetCore.Diagnostics</li>
        <li>Microsoft.AspNetCore.Diagnostics.HealthChecks</li>
        <li>Microsoft.AspNetCore.HostFiltering</li>
        <li>Microsoft.AspNetCore.Hosting</li>
        <li>Microsoft.AspNetCore.Hosting.Abstractions</li>
        <li>Microsoft.AspNetCore.Hosting.Server.Abstractions</li>
        <li>Microsoft.AspNetCore.Http</li>
        <li>Microsoft.AspNetCore.Http.Abstractions</li>
        <li>Microsoft.AspNetCore.Http.Connections</li>
        <li>Microsoft.AspNetCore.Http.Extensions</li>
        <li>Microsoft.AspNetCore.HttpOverrides</li>
        <li>Microsoft.AspNetCore.HttpsPolicy</li>
        <li>AspNetCore。Identity</li>
        <li>Microsoft.AspNetCore.Localization</li>
        <li>Microsoft.AspNetCore.Localization.Routing</li>
        <li>Microsoft.AspNetCore.Mvc</li>
        <li>Microsoft.AspNetCore.Mvc.Abstractions</li>
        <li>Microsoft.AspNetCore.Mvc.Analyzers</li>
        <li>Microsoft.AspNetCore.Mvc.ApiExplorer</li>
        <li>Microsoft.AspNetCore.Mvc.Api.Analyzers</li>
        <li>Microsoft.AspNetCore.Mvc.Core</li>
        <li>Microsoft.AspNetCore.Mvc.Cors</li>
        <li>Microsoft.AspNetCore.Mvc.DataAnnotations</li>
        <li>Microsoft.AspNetCore.Mvc.Formatters.Json</li>
        <li>Microsoft.AspNetCore.Mvc.Formatters.Xml</li>
        <li>Microsoft.AspNetCore.Mvc.Localization</li>
        <li>AspNetCore。Razor</li>
        <li>AspNetCore Razor 。>microsoft.aspnetcore.mvc.razor.viewcompilation</li>
        <li>AspNetCore。 Razor頁面</li>
        <li>Microsoft.AspNetCore.Mvc.TagHelpers</li>
        <li>Microsoft.AspNetCore.Mvc.ViewFeatures</li>
        <li>AspNetCore。Razor</li>
        <li>AspNetCore Razor 。運行</li>
        <li>AspNetCore Razor 。設計</li>
        <li>Microsoft.AspNetCore.ResponseCaching</li>
        <li>Microsoft.AspNetCore.ResponseCaching.Abstractions</li>
        <li>Microsoft.AspNetCore.ResponseCompression</li>
        <li>Microsoft.AspNetCore.Rewrite</li>
        <li>Microsoft.AspNetCore.Routing</li>
        <li>Microsoft.AspNetCore.Routing.Abstractions</li>
        <li>Microsoft.AspNetCore.Server.HttpSys</li>
        <li>Microsoft.AspNetCore.Server.IIS</li>
        <li>Microsoft.AspNetCore.Server.IISIntegration</li>
        <li>Microsoft.AspNetCore.Server.Kestrel</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Core</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Https</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions</li>
        <li>Microsoft.AspNetCore.Server.Kestrel.Transport.Sockets</li>
        <li>Microsoft.AspNetCore.Session</li>
        <li>AspNetCore。SignalR</li>
        <li>AspNetCore SignalR 。核心</li>
        <li>Microsoft.AspNetCore.StaticFiles</li>
        <li>Microsoft.AspNetCore.WebSockets</li>
        <li>Microsoft.AspNetCore.WebUtilities</li>
        <li>Microsoft.Net.Http.Headers </li>
    </ul>
</details>

### <a name="review-breaking-changes"></a>檢查重大變更

[檢查重大變更](#break)

### <a name="framework-reference"></a>架構參考

您可以透過上述其中一個套件取得 ASP.NET Core 的功能，作為 `Microsoft.AspNetCore.App` 共用架構的一部分。 *共用架構*是安裝在電腦上的一組元件 (*.dll*檔案) ，其中包含執行時間元件和目標套件。 如需詳細資訊，請參閱[共用的架構](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) \(英文\)。

* 以 SDK 為目標的專案會 `Microsoft.NET.Sdk.Web` 隱含地參考 `Microsoft.AspNetCore.App` 架構。

  這些專案不需要額外的參考：

  ```xml
  <Project Sdk="Microsoft.NET.Sdk.Web">
    <PropertyGroup>
      <TargetFramework>netcoreapp3.0</TargetFramework>
    </PropertyGroup>
      ...
  </Project>
  ```

* `Microsoft.NET.Sdk`以或 SDK 為目標的專案 `Microsoft.NET.Sdk.Razor` 應該將明確加入 `FrameworkReference` 至 `Microsoft.AspNetCore.App` ：

  ```xml
  <Project Sdk="Microsoft.NET.Sdk.Razor">
    <PropertyGroup>
      <TargetFramework>netcoreapp3.0</TargetFramework>
    </PropertyGroup>

    <ItemGroup>
      <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>
      ...
  </Project>
  ```

#### <a name="framework-dependent-builds-using-docker"></a>使用 Docker 的 Framework 相依組建

以 Framework 相依的主控台應用程式組建，使用相依于 ASP.NET Core [共用架構](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) 的套件可能會提供下列執行階段錯誤：

```console
It was not possible to find any compatible framework version
The specified framework 'Microsoft.AspNetCore.App', version '3.0.0' was not found.
  - No frameworks were found.
```

`Microsoft.AspNetCore.App` 是包含 ASP.NET Core 執行時間，而且只存在於 [dotnet/Core/aspnet](https://hub.docker.com/_/microsoft-dotnet-core-aspnet/) Docker 映射上的共用架構。 3.0 SDK 會使用 ASP.NET Core 來減少 framework 相依組建的大小，方法是不包含共用架構中所提供的重複程式庫複本。 這可能會節省最多 18 MB，但需要 ASP.NET Core 執行時間存在/安裝，才能執行應用程式。

若要判斷應用程式是否相依 (ASP.NET Core 共用架構上的直接或間接) ，請檢查應用程式組建/發佈期間產生的檔案 *runtimeconfig.js* 。 下列 JSON 檔案顯示 ASP.NET Core 共用架構的相依性：

```json
{
  "runtimeOptions": {
    "tfm": "netcoreapp3.0",
    "framework": {
      "name": "Microsoft.AspNetCore.App",
      "version": "3.0.0"
    },
    "configProperties": {
      "System.GC.Server": true
    }
  }
}
```

如果您的應用程式使用 Docker，請使用包含 ASP.NET Core 3.0 的基底映射。 例如： `docker pull mcr.microsoft.com/dotnet/core/aspnet:3.0` 。

### <a name="add-package-references-for-removed-assemblies"></a>新增已移除元件的套件參考

ASP.NET Core 3.0 會移除先前屬於封裝參考一部分的元件 `Microsoft.AspNetCore.App` 。 若要將已移除的元件視覺化，請比較兩個共用的架構資料夾。 例如，2.2.7 和3.0.0 版本的比較：

![共用架構元件比較](22-to-30/_static/assembly-diff.png)

若要繼續使用已移除元件所提供的功能，請參考對應套件的3.0 版本：

* 範本產生的具有 **個別使用者帳戶** 的 web 應用程式需要新增下列套件：

  [!code-xml[](22-to-30/samples/WebFull.csproj?highlight=9-13)]

* [Microsoft.EntityFrameworkCore](https://www.nuget.org/packages/Microsoft.EntityFrameworkCore)

  如需參考資料庫提供者特定封裝的詳細資訊，請參閱 [資料庫提供者](/ef/core/providers/index)。

* Identity UI

  您可以藉由參考 AspNetCore 來新增[ Identity UI](xref:security/authentication/identity)的支援。 [ IdentityUI](https://www.nuget.org/packages/Microsoft.AspNetCore.Identity.UI)套件。

* SPA 服務

  * [AspNetCore. SpaServices](https://www.nuget.org/packages/Microsoft.AspNetCore.SpaServices)
  * [AspNetCore. SpaServices 延伸模組](https://www.nuget.org/packages/Microsoft.AspNetCore.SpaServices.Extensions)

* 驗證：支援協力廠商驗證流程可作為 NuGet 套件：

  * Facebook OAuth ([AspNetCore](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Facebook)) 
  * Google OAuth ([AspNetCore 驗證 google](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Google)) 
  * Microsoft 帳戶驗證 ([AspNetCore MicrosoftAccount](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.MicrosoftAccount)) 
  *  (AspNetCore 的 OpenID Connect authentication [OpenIdConnect](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.OpenIdConnect)) 
  * OpenID Connect 持有人權杖 ([AspNetCore JwtBearer](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.JwtBearer)) 
  * Twitter OAuth ([AspNetCore。 twitter](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.Twitter)) 
  * WsFederation authentication ([AspNetCore WsFederation](https://www.nuget.org/packages/Microsoft.AspNetCore.Authentication.WsFederation)) 

* 的格式設定和內容協商支援 `System.Net.HttpClient` ： [WebApi。用戶端](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet 套件提供 `System.Net.HttpClient` 與 api （例如和）的實用擴充性 `ReadAsAsync` `PostJsonAsync` 。

* Razor 執行時間編譯：對 views 和頁面的執行時間編譯支援 Razor 現在是 AspNetCore 的一部分。 [ Razor>microsoft.aspnetcore.mvc.razor.runtimecompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation)。

* MVC `Newtonsoft.Json` (Json.NET) 支援：使用 mvc 搭配的支援 `Newtonsoft.Json` 現在是的一部分 [`Microsoft.AspNetCore.Mvc.NewtonsoftJson`](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson) 。

## <a name="startup-changes"></a>啟動變更

下圖顯示 ASP.NET Core 2.2 Razor 頁面 Web 應用程式中已刪除及已變更的行：

![ASP.NET Core 2.2：：： no-loc (Razor) ：：： Web 應用程式中已刪除和變更的行](22-to-30/_static/startup2.2.png)

在上圖中，刪除的程式碼以紅色顯示。 刪除的程式碼並不會顯示 cookie 在比較檔案之前刪除的選項代碼。

下圖顯示 ASP.NET Core 3.0 Razor 頁面 Web 應用程式中新增和變更的行：

![在 ASP.NET Core 3.0：：： no-loc (Razor) ：：： Web 應用程式中新增和變更的行](22-to-30/_static/startup3.0.png)

在上圖中，已加入的程式碼會以綠色顯示。 如需下列變更的相關資訊：

* `services.AddMvc` 若為 `services.AddRazorPages` ，請參閱本檔中的 [MVC 服務註冊](#mvc-service-registration) 。
* `CompatibilityVersion`，請參閱 <xref:mvc/compatibility-version> 。
* `IHostingEnvironment` 若要 `IWebHostEnvironment` 瞭解，請參閱 [此 GitHub 公告](https://github.com/dotnet/AspNetCore/issues/7749)。
* `app.UseAuthorization` 已新增至範本，以顯示必須新增訂單授權中介軟體。 如果應用程式不使用授權，您可以安全地移除對的呼叫 `app.UseAuthorization` 。
* `app.UseEndpoints`，請參閱本檔中的[ Razor 頁面](#razor-pages)或[遷移 Startup.Configu) ](#migrate-startupconfigure) 。

### <a name="analyzer-support"></a>分析器支援

`Microsoft.NET.Sdk.Web`以隱含參考分析器為目標的專案，先前隨附于[AspNetCore](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Analyzers/)封裝中。 不需要額外的參考來啟用這些。

如果您的應用程式使用先前使用[AspNetCore](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Api.Analyzers/)隨附的[api 分析器](xref:web-api/advanced/analyzers)，請編輯您的專案檔，以參考隨附于 .net Core Web SDK 的分析器：

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">
    <PropertyGroup>
        <TargetFramework>netcoreapp3.0</TargetFramework>
        <IncludeOpenAPIAnalyzers>true</IncludeOpenAPIAnalyzers>
    </PropertyGroup>

    ...
</Project>
```

### <a name="no-locrazor-class-library"></a>Razor 類別庫

Razor 提供 MVC UI 元件的類別庫專案必須設定專案檔 `AddRazorSupportForMvc` 中的屬性：

```xml
<PropertyGroup>
  <AddRazorSupportForMvc>true</AddRazorSupportForMvc>
</PropertyGroup>
```

### <a name="in-process-hosting-model"></a>同處理序裝載模型

專案預設為 ASP.NET Core 3.0 或更新版本中的同 [進程裝載模型](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) 。 您可以選擇性地移除 `<AspNetCoreHostingModel>` 專案檔中的屬性（如果其值為） `InProcess` 。

## <a name="kestrel"></a>Kestrel

### <a name="configuration"></a>組態

將 Kestrel 設定遷移至 (Program.cs) 所提供的[web 主機](#hostb)建立器 `ConfigureWebHostDefaults` ： *Program.cs*

```csharp
public static IHostBuilder CreateHostBuilder(string[] args) =>
    Host.CreateDefaultBuilder(args)
        .ConfigureWebHostDefaults(webBuilder =>
        {
            webBuilder.ConfigureKestrel(serverOptions =>
            {
                // Set properties and call methods on options
            })
            .UseStartup<Startup>();
        });
```

如果應用程式以手動方式建立主機 `HostBuilder` ，請在 `UseKestrel` 中的 web 主機建立器上呼叫 `ConfigureWebHostDefaults` ：

```csharp
public static void Main(string[] args)
{
    var host = new HostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .ConfigureWebHostDefaults(webBuilder =>
        {
            webBuilder.UseKestrel(serverOptions =>
            {
                // Set properties and call methods on options
            })
            .UseIISIntegration()
            .UseStartup<Startup>();
        })
        .Build();

    host.Run();
}
```

### <a name="connection-middleware-replaces-connection-adapters"></a>連接中介軟體會取代連接介面卡

已 `Microsoft.AspNetCore.Server.Kestrel.Core.Adapter.Internal.IConnectionAdapter` 從 Kestrel 中移除連接介面卡 () 。 以連接中介軟體取代連接介面卡。 連接中介軟體類似于 ASP.NET Core 管線中的 HTTP 中介軟體，但較低層級的連接。 HTTPS 和連接記錄：

* 已從連接配接器移至連線中介軟體。
* 這些擴充方法的運作方式與舊版 ASP.NET Core 一樣。 

如需詳細資訊，請參閱 [Kestrel 文章 >listenoptions 一節中的 TlsFilterConnectionHandler 範例](../fundamentals/servers/kestrel.md?view=aspnetcore-3.0#listenoptionsprotocols)。

### <a name="transport-abstractions-moved-and-made-public"></a>傳輸抽象層已移動並公開

Kestrel 傳輸層已公開為中的公用介面 `Connections.Abstractions` 。 作為這些更新的一部分：

* `Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions` 和相關聯的類型已移除。
* <xref:Microsoft.AspNetCore.Server.Kestrel.KestrelServerOptions.NoDelay> 已從移 <xref:Microsoft.AspNetCore.Server.Kestrel.Core.ListenOptions> 至傳輸選項。
* `Microsoft.AspNetCore.Server.Kestrel.Transport.Abstractions.Internal.SchedulingMode` 已從移除 <xref:Microsoft.AspNetCore.Server.Kestrel.KestrelServerOptions> 。

如需詳細資訊，請參閱下列 GitHub 資源：

* [用戶端/伺服器網路抽象概念 (dotnet/AspNetCore #10308) ](https://github.com/dotnet/AspNetCore/issues/10308)
* [在 top (dotnet/AspNetCore #10321 上執行新的探源接聽程式抽象概念和重新平臺 Kestrel) ](https://github.com/dotnet/AspNetCore/pull/10321)

### <a name="kestrel-request-trailer-headers"></a>Kestrel 要求尾端標頭

針對以舊版 ASP.NET Core 為目標的應用程式：

* Kestrel 會將 HTTP/1.1 區塊尾端標頭新增至要求標頭集合。
* 要求主體讀取至結尾之後，就可以使用尾端。

這會導致在標頭和結尾之間有一些不明確的問題，因此尾端已移至新的集合 (`RequestTrailerExtensions`) 在3.0 中。

HTTP/2 要求尾端為：

* 在 ASP.NET Core 2.2 中無法使用。
* 以3.0 為形式提供 `RequestTrailerExtensions` 。

有新的要求延伸方法可存取這些尾端。 和 HTTP/1.1 一樣，在要求主體讀取至結尾之後可使用尾端。

在3.0 版本中， `RequestTrailerExtensions` 可以使用下列方法：

* `GetDeclaredTrailers`：取得要求 `Trailer` 標頭，此標頭會列出主體之後所預期的尾端。
* `SupportsTrailers`：指出要求是否支援接收尾端標頭。
* `CheckTrailersAvailable`：檢查要求是否支援結尾和是否可讀取。 這種檢查不會假設有尾端可供讀取。 即使這個方法傳回，也可能不會讀取任何尾端 `true` 。
* `GetTrailer`：從回應取得要求的尾端標頭。 `SupportsTrailers`在呼叫之前檢查 `GetTrailer` ， <xref:System.NotSupportedException> 如果要求不支援尾端標頭，則可能會發生。

如需詳細資訊，請參閱 [將要求尾端放在個別的集合中 (dotnet/AspNetCore #10410) ](https://github.com/dotnet/AspNetCore/pull/10410)。

### <a name="allowsynchronousio-disabled"></a>AllowSynchronousIO 已停用

`AllowSynchronousIO` 啟用或停用同步 i/o Api，例如 `HttpRequest.Body.Read` 、 `HttpResponse.Body.Write` 和 `Stream.Flush` 。 這些 Api 是執行緒耗盡的來源，導致應用程式損毀。 在3.0 中， `AllowSynchronousIO` 預設為停用。 如需詳細資訊，請參閱 [Kestrel 文章中的同步 i/o 一節](../fundamentals/servers/kestrel.md?view=aspnetcore-3.0#synchronous-io)。

如果需要同步 i/o，您可以 `AllowSynchronousIO` 在呼叫時（ `ConfigureKestrel` 例如，如果使用 Kestrel) ）在 (使用的伺服器上設定選項來啟用它。 請注意，伺服器 (Kestrel、HttpSys、TestServer 等 ) 全部都有自己 `AllowSynchronousIO` 的選項，而不會影響到其他伺服器。 您可以使用下列選項，以每個要求為基礎來啟用所有伺服器的同步 i/o `IHttpBodyControlFeature.AllowSynchronousIO` ：

```csharp
var syncIOFeature = HttpContext.Features.Get<IHttpBodyControlFeature>();

if (syncIOFeature != null)
{
    syncIOFeature.AllowSynchronousIO = true;
}
```

如果您對於 <xref:System.IO.TextWriter> 在 [Dispose](/dotnet/standard/garbage-collection/implementing-dispose)中呼叫同步 api 的執行或其他串流有問題，請改為呼叫新的 <xref:System.IO.Stream.DisposeAsync*> api。

如需詳細資訊，請參閱 [所有伺服器中的 [公告] AllowSynchronousIO 已停用 (dotnet/AspNetCore #7644) ](https://github.com/dotnet/AspNetCore/issues/7644)。

### <a name="output-formatter-buffering"></a>輸出格式器緩衝

[Newtonsoft.Json](https://www.newtonsoft.com/json)、 <xref:System.Xml.Serialization.XmlSerializer> 和基礎的輸出格式器 <xref:System.Runtime.Serialization.DataContractSerializer> 只支援同步序列化。 為了讓這些格式器可以使用伺服器的 [AllowSynchronousIO](https://github.com/dotnet/aspnetcore/issues/7644) 限制，MVC 會在寫入磁片之前緩衝處理這些格式器的輸出。 由於緩衝處理的結果，MVC 會在使用這些格式器回應時包含 Content-type 標頭。

<xref:System.Text.Json> 支援非同步序列化，因此格式的格式器不 `System.Text.Json` 會緩衝。 請考慮使用此格式器以改善效能。

若要停用緩衝，應用程式可以 <xref:Microsoft.AspNetCore.Mvc.MvcOptions.SuppressOutputFormatterBuffering> 在啟動時設定：

```csharp
services.AddControllers(options => options.SuppressOutputFormatterBuffering = true)
```

請注意，如果尚未設定，這可能會導致應用程式擲回執行時間例外狀況 `AllowSynchronousIO` 。

### <a name="microsoftaspnetcoreserverkestrelhttps-assembly-removed"></a>已移除 AspNetCore Kestrel. Https 元件

在 ASP.NET Core 2.1 中， *Microsoft.AspNetCore.Server.Kestrel.Https.dll* 的內容已移至 *Microsoft.AspNetCore.Server.Kestrel.Core.dll*。 這是使用屬性的非重大更新 `TypeForwardedTo` 。 若為3.0，則會移除空白 *Microsoft.AspNetCore.Server.Kestrel.Https.dll* 元件和 NuGet 套件。

參考 [AspNetCore](https://www.nuget.org/packages/Microsoft.AspNetCore.Server.Kestrel.Https) 的程式庫應該將 ASP.NET Core 相依性更新為2.1 或更新版本。

以 ASP.NET Core 2.1 或更新版本為目標的應用程式和程式庫應移除 Kestrel 的任何直接參考 [AspNetCore](https://www.nuget.org/packages/Microsoft.AspNetCore.Server.Kestrel.Https) 。

<a id="jsonnet-support"></a>

## <a name="newtonsoftjson-jsonnet-support"></a>Newtonsoft.Js(Json.NET) 支援

在 [改善 ASP.NET Core 共用架構](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)的工作中， [Newtonsoft.Js(Json.NET) ](https://www.newtonsoft.com/json/help/html/Introduction.htm) 已經從 ASP.NET Core 共用架構中移除。

ASP.NET Core 的預設 JSON 序列化程式現在是 <xref:System.Text.Json> .Net Core 3.0 中的新功能。 盡可能考慮使用 `System.Text.Json` 。 它是高效能，不需要額外的程式庫相依性。 不過，由於 `System.Text.Json` 是新的，因此目前可能遺失您的應用程式所需的功能。 如需詳細資訊，請參閱 [如何從 Newtonsoft.Js移至 System.Text.Js](/dotnet/standard/serialization/system-text-json-migrate-from-newtonsoft-how-to)。

### <a name="use-newtonsoftjson-in-an-aspnet-core-30-no-locsignalr-project"></a>在 ASP.NET Core 3.0 專案中使用 Newtonsoft.Json SignalR

* 安裝 [AspNetCore。 SignalR>microsoft.aspnetcore.mvc.newtonsoftjson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet 套件。

* 在用戶端上，將 `AddNewtonsoftJsonProtocol` 方法呼叫連結至 `HubConnectionBuilder` 實例：

  ```csharp
  new HubConnectionBuilder()
      .WithUrl("/chathub")
      .AddNewtonsoftJsonProtocol(...)
      .Build();
  ```

* 在伺服器上，將 `AddNewtonsoftJsonProtocol` 方法呼叫連結至 `AddSignalR` 中的方法呼叫 `Startup.ConfigureServices` ：

  ```csharp
  services.AddSignalR()
      .AddNewtonsoftJsonProtocol(...);
  ```

### <a name="use-newtonsoftjson-in-an-aspnet-core-30-mvc-project"></a>在 ASP.NET Core 3.0 MVC 專案中使用 Newtonsoft.Json

* 安裝 [`Microsoft.AspNetCore.Mvc.NewtonsoftJson`](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson) 套件。

* `Startup.ConfigureServices`要呼叫 `AddNewtonsoftJson` 的更新。

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  `AddNewtonsoftJson` 與新的 MVC 服務註冊方法相容：

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  `Newtonsoft.Json` 設定可以在呼叫中設定 `AddNewtonsoftJson` ：

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

  **注意：** 如果 `AddNewtonsoftJson` 無法使用此方法，請確定您已安裝 [`Microsoft.AspNetCore.Mvc.NewtonsoftJson`](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson) 套件。 常見的錯誤是 [ 在套件上安裝Newtonsoft.Js](https://www.nuget.org/packages/Newtonsoft.Json/) ，而不是 [`Microsoft.AspNetCore.Mvc.NewtonsoftJson`](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson) 封裝。

如需詳細資訊，請參閱 [新增 Newtonsoft.Js的 JSON 格式支援](xref:web-api/advanced/formatting#add-newtonsoftjson-based-json-format-support)。

## <a name="mvc-service-registration"></a>MVC 服務註冊

ASP.NET Core 3.0 新增了在內部註冊 MVC 案例的新選項 `Startup.ConfigureServices` 。

有三個與 MVC 案例相關的最新高階擴充方法 `IServiceCollection` 可供使用。 範本會使用這些新的方法，而不是 `AddMvc` 。 不過， `AddMvc` 其行為會與先前版本中的行為相同。

下列範例會新增控制器和 API 相關功能的支援，但不支援視圖或頁面。 API 範本會使用下列程式碼：

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

下列範例會新增控制器、API 相關功能和視圖的支援，但不支援頁面。 Web 應用程式 (MVC) 範本會使用下列程式碼：

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

下列範例會新增頁面的支援 Razor ，以及最基本的控制器支援。 Web 應用程式範本會使用下列程式碼：

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

您也可以結合新的方法。 下列範例相當於 `AddMvc` 在 ASP.NET Core 2.2 中呼叫：

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
    services.AddRazorPages();
}
```

## <a name="routing-startup-code"></a>路由啟動程式碼

如果應用程式呼叫 `UseMvc` 或 `UseSignalR` ，可能的話，請將應用程式遷移至 [端點路由](xref:fundamentals/routing) 。 為了改進與舊版 MVC 的端點路由相容性，我們已還原 ASP.NET Core 2.2 中引進的 URL 產生變更。 如果您在2.2 中使用端點路由時遇到問題，ASP.NET Core 3.0 中的增強功能，但有下列例外狀況：

* 如果應用程式在 `IRouter` 中執行或繼承 `Route` ，請使用 [DynamicRouteValuesTransformer](https://github.com/dotnet/AspNetCore.Docs/issues/12997) 做為取代。
* 如果應用程式直接在 `RouteData.Routers` MVC 內部存取來剖析 url，您可以使用 [LinkParser. ParsePathByEndpointName](xref:Microsoft.AspNetCore.Routing.LinkParserEndpointNameAddressExtensions.ParsePathByEndpointName*)取代此項。 
  * 使用路由名稱定義路由。
  * 使用 `LinkParser.ParsePathByEndpointName` ，並傳入所需的路由名稱。

端點路由支援與相同的路由模式語法和路由模式撰寫功能 `IRouter` 。 端點路由支援 `IRouteConstraint` 。 端點路由支援 `[Route]` 、 `[HttpGet]` 和其他 MVC 路由屬性。

針對大部分的應用程式，只 `Startup` 需要變更。

### <a name="migrate-startupconfigure"></a>遷移 Startup.Configu) 

一般建議：

* 加入 `UseRouting`。
* 如果應用程式呼叫 `UseStaticFiles` ，請 `UseStaticFiles` **在之前**放置 `UseRouting` 。
* 如果應用程式使用驗證/授權功能（例如 `AuthorizePage` 或 `[Authorize]` ），請在下列位置呼叫 `UseAuthentication` and `UseAuthorization` ： **after**、 `UseRouting` and。 `UseCors` `UseEndpoints`

  ```csharp
  public void Configure(IApplicationBuilder app)
  {
    ...

    app.UseStaticFiles();

    app.UseRouting();
    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints => {
       endpoints.MapControllers();
    });
  ```

* 將 `UseMvc` 或取代 `UseSignalR` 為 `UseEndpoints` 。
* 如果應用程式使用 [cors](xref:security/cors) 案例（例如），請在 `[EnableCors]` `UseCors` 使用 cors 的任何其他中介軟體之前放置呼叫 (例如， `UseCors` 在 `UseAuthentication` 、 `UseAuthorization` 和) 之前放置 `UseEndpoints` 。
* 將取代為 `IHostingEnvironment` `IWebHostEnvironment` ，並加入 `using` <xref:Microsoft.Extensions.Hosting?displayProperty=fullName> 命名空間的語句。
* 取代 `IApplicationLifetime` 為 <xref:Microsoft.Extensions.Hosting.IHostApplicationLifetime> (<xref:Microsoft.Extensions.Hosting?displayProperty=fullName> 命名空間) 。
* 取代 `EnvironmentName` 為 <xref:Microsoft.Extensions.Hosting.Environments> (<xref:Microsoft.Extensions.Hosting?displayProperty=fullName> 命名空間) 。

下列程式碼是 `Startup.Configure` 典型 ASP.NET Core 2.2 應用程式中的範例：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

更新先前的程式 `Startup.Configure` 代碼之後：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();

    app.UseRouting();

    app.UseCors();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

> [!WARNING]
> 對於大部分的應用程式，呼叫 `UseAuthentication` 、和 `UseAuthorization` `UseCors` 必須在的呼叫與之間出現，才能 `UseRouting` `UseEndpoints` 生效。

### <a name="health-checks"></a>健康情況檢查

健康情況檢查會使用端點路由搭配一般主機。 在中 `Startup.Configure` ， `MapHealthChecks` 使用端點 URL 或相對路徑呼叫端點產生器：

```csharp
app.UseEndpoints(endpoints =>
{
    endpoints.MapHealthChecks("/health");
});
```

健康情況檢查端點可以：

* 指定一或多個允許的主機/埠。
* 需要授權。
* 需要 CORS。

如需詳細資訊，請參閱<xref:host-and-deploy/health-checks>。

### <a name="security-middleware-guidance"></a>安全性中介軟體指引

對授權和 CORS 的支援是與 [中介軟體](xref:fundamentals/middleware/index) 方法整合的。 這可讓您在這些案例中使用相同的中介軟體和功能。 此版本提供了更新的授權中介軟體，並增強了 CORS 中介軟體，讓它能夠瞭解 MVC 控制器所使用的屬性。

#### <a name="cors"></a>CORS

先前，CORS 可能很難設定。 已提供中介軟體以用於某些使用案例，但 MVC 篩選準則的用途是在其他使用案例中， **而不** 使用中介軟體。 在 ASP.NET Core 3.0 中，我們建議所有需要 CORS 的應用程式都使用 CORS 中介軟體搭配端點路由。 `UseCors` 可以使用預設原則來提供，而且 `[EnableCors]` 和 `[DisableCors]` 屬性可用來覆寫預設原則（必要時）。

在下例中︰

* 已針對具有命名原則的所有端點啟用 CORS `default` 。
* `MyController`類別會使用屬性來停用 CORS `[DisableCors]` 。

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default");

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a>授權

在舊版 ASP.NET Core 中，授權支援是透過 `[Authorize]` 屬性提供。 未提供授權中介軟體。 在 ASP.NET Core 3.0 中，需要授權中介軟體。 建議您立即將 ASP.NET Core 授權中介軟體 (`UseAuthorization`) `UseAuthentication` 。 授權中介軟體也可以使用可覆寫的預設原則進行設定。

在 ASP.NET Core 3.0 或更新版本中， `UseAuthorization` 會在中呼叫， `Startup.Configure` 而下列程式 `HomeController` 要求已登入的使用者：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : Controller
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

使用端點路由時，建議您不要設定 `<xref:Microsoft.AspNetCore.Mvc.Authorization.AuthorizeFilter>` ，而改為依賴授權中介軟體。  如果應用程式 `AuthorizeFilter` 在 MVC 中使用做為全域篩選，我們建議您重構程式碼，以在的呼叫中提供原則 `AddAuthorization` 。

`DefaultPolicy`最初設定為需要驗證，因此不需要進行其他設定。 在下列範例中，MVC 端點會標示為， `RequireAuthorization` 讓所有要求都必須根據來授權 `DefaultPolicy` 。 不過，在 `HomeController` 不使用使用者登入應用程式的情況下允許存取，原因 `[AllowAnonymous]` 如下：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute().RequireAuthorization();
    });
}

[AllowAnonymous]
public class HomeController : Controller
{
    ...
}
```

### <a name="authorization-for-specific-endpoints"></a>特定端點的授權

您也可以針對特定的端點類別設定授權。 下列程式碼範例會將設定為全域的 MVC 應用程式轉換 `AuthorizeFilter` 為具有需要授權之特定原則的應用程式：

[!code-csharp[](22-to-30/samples/Startup.cs?name=snippet&highlight=8-9,21-22,26-27,53-54)]

也可以自訂原則。 `DefaultPolicy`設定為需要驗證：

[!code-csharp[](22-to-30/samples/Startup2.cs?name=snippet&highlight=21-26,52)]

[!code-csharp[](22-to-30/samples/HomeController.cs?name=snippet)]

或者，所有端點都可以設定為要求授權，而不需要 `[Authorize]` 或藉 `RequireAuthorization` 由設定 `FallbackPolicy` 。 與 `FallbackPolicy` 不同 `DefaultPolicy` 。 `DefaultPolicy`是由或觸發 `[Authorize]` `RequireAuthorization` ，而 `FallbackPolicy` 當沒有設定其他原則時，就會觸發。 `FallbackPolicy` 最初是設定為允許不具授權的要求。

下列範例與上述範例相同，但會 `DefaultPolicy` 使用在 `FallbackPolicy` 所有端點上一律需要驗證，但指定時除外 `[AllowAnonymous]` ：

```csharp
public void ConfigureServices(IServiceCollection services)
{
    ...

    services.AddAuthorization(options =>
    {
        options.FallbackPolicy = new AuthorizationPolicyBuilder()
          .RequireAuthenticatedUser()
          .Build();
    });
}

public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : Controller
{
    ...
}
```

依中介軟體授權的運作方式，不需具備任何特定授權知識的架構。 例如， [健康情況檢查](xref:host-and-deploy/health-checks) 沒有授權的特定知識，但健康狀態檢查可以有中介軟體所套用的可設定授權原則。

此外，每個端點都可以自訂其授權需求。 在下列範例中，會 `UseAuthorization` 使用來處理授權 `DefaultPolicy` ，但 `/healthz` 健康情況檢查端點需要 `admin` 使用者：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

在某些情況下會執行保護。 如果因為缺少中介軟體而略過授權或 CORS 原則時，端點中介軟體會擲回例外狀況。 分析器支援，以提供有關設定錯誤的其他意見反應。

#### <a name="custom-authorization-handlers"></a>自訂授權處理常式

如果應用程式使用自訂 [授權處理常式](xref:security/authorization/policies#authorization-handlers)，則端點路由會將不同的資源類型傳遞給處理常式，而不是 MVC。 預期授權處理常式內容資源屬於 <xref:Microsoft.AspNetCore.Mvc.Filters.AuthorizationFilterContext> ([MVC 篩選器所提供](xref:security/authorization/policies#access-mvc-request-context-in-handlers) 之資源類型的處理常式) 將需要更新，以處理 <xref:Microsoft.AspNetCore.Routing.RouteEndpoint> 由端點路由) 提供給授權處理常式的資源類型 (類型的資源。

MVC 仍 `AuthorizationFilterContext` 會使用資源，因此，如果應用程式使用 MVC 授權篩選和端點路由授權，則可能需要處理這兩種類型的資源。

### SignalR

中樞的對應 SignalR 現在會在內部進行 `UseEndpoints` 。

使用對應每個中樞 `MapHub` 。 如同先前的版本，每個中樞都已明確列出。

在下列範例中， `ChatHub` SignalR 會新增中樞的支援：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

有一個新的選項，可控制用戶端的訊息大小限制。 例如，在 `Startup.ConfigureServices` 中：

```csharp
services.AddSignalR(hubOptions =>
{
    hubOptions.MaximumReceiveMessageSize = 32768;
});
```

在 ASP.NET Core 2.2 中，您可以設定 `TransportMaxBufferSize` 和，以有效地控制訊息大小上限。 在 ASP.NET Core 3.0 中，該選項現在只會控制觀察到背壓之前的大小上限。

### <a name="mvc-controllers"></a>MVC 控制器

控制器的對應現在會在內部進行 `UseEndpoints` 。

`MapControllers`如果應用程式使用屬性路由，請新增。 因為路由包含 ASP.NET Core 3.0 或更新版本中許多架構的支援，所以新增屬性路由控制器是加入宣告的。

取代下列項目：

* `MapControllerRoute` 的 `MapRoute`
* `MapAreaControllerRoute` 的 `MapAreaRoute`

由於路由現在支援的不只是 MVC，因此術語已經改變，讓這些方法清楚陳述它們的用途。 慣例路由（例如） `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` 會依其新增的順序套用。  (放置更明確的路由，例如區域) 的路由。

在下例中︰

* `MapControllers` 新增屬性路由控制器的支援。
* `MapAreaControllerRoute` 在區域中新增控制器的傳統路由。
* `MapControllerRoute` 新增控制器的一般路由。

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin",
            "admin",
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

#### <a name="async-suffix-removal-from-controller-action-names"></a>從控制器動作名稱移除非同步尾碼

在 ASP.NET Core 3.0 中，ASP.NET Core MVC 會 `Async` 從控制器動作名稱移除尾碼。 路由和連結產生都會受到這個新的預設值所影響。 例如：

```csharp
public class ProductsController : Controller
{
    public async Task<IActionResult> ListAsync()
    {
        var model = await _dbContext.Products.ToListAsync();
        return View(model);
    }
}
```

在 ASP.NET Core 3.0 之前：

* 您可以在 *Products/metrics.listasync* route 存取上述動作。
* 需要指定尾碼的連結產生 `Async` 。 例如：

    ```cshtml
    <a asp-controller="Products" asp-action="ListAsync">List</a>
    ```

在 ASP.NET Core 3.0：

* 您可以在 *Products/List* route 存取上述動作。
* 連結產生不需要指定 `Async` 尾碼。 例如：

    ```cshtml
    <a asp-controller="Products" asp-action="List">List</a>
    ```

此變更不會影響使用屬性指定的名稱 [`[ActionName]`](/dotnet/api/microsoft.aspnetcore.mvc.actionnameattribute) 。 您可以使用中的下列程式碼來停用預設行為 `Startup.ConfigureServices` ：

```csharp
services.AddMvc(options =>
    options.SuppressAsyncSuffixInActionNames = false);
```

#### <a name="changes-to-link-generation"></a>連結產生的變更

如同 [舊版路由的差異](xref:fundamentals/routing#differences-from-earlier-versions-of-routing)檔中所述，使用和類似的 api (連結產生有一些差異 `Url.Link` ，例如) 。 其中包括：

* 根據預設，使用端點路由時，不一定會保留所產生之 Uri 中的路由參數大小寫。 此行為可以使用介面來控制 `IOutboundParameterTransformer` 。
* 產生無效路由的 URI (控制器/動作或不存在的頁面) 將會在端點路由下產生空字串，而不是產生不正確 URI。
* 從目前內容 (路由參數的環境值，不會在與端點路由的連結產生中自動使用) 。 先前，當 (或頁面) 產生另一個動作的連結時，會從 *目前* 的路由環境值推斷未指定的路由值。 使用端點路由時，必須在連結產生期間明確指定所有路由參數。

### <a name="no-locrazor-pages"></a>Razor 頁面

對應 Razor 頁面現在會在內部進行 `UseEndpoints` 。

`MapRazorPages`如果應用程式使用頁面，請新增 Razor 。 由於端點路由包含許多架構的支援， Razor 現在加入頁面是加入宣告的。

在下列 `Startup.Configure` 方法中， `MapRazorPages` 新增了頁面的支援 Razor ：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a>使用不含端點路由的 MVC

在 `UseMvc` ASP.NET Core 3.0 中透過或使用 MVC `UseMvcWithDefaultRoute` 時，需要明確加入 `Startup.ConfigureServices` 。 這是必要的，因為 MVC 必須知道它是否可以在初始化期間依賴授權和 CORS 中介軟體。 如果應用程式嘗試使用不支援的設定，則會提供分析器來發出警告。

如果應用程式需要舊版 `IRouter` 支援，請 `EnableEndpointRouting` 在中使用下列任何一種方法來停用 `Startup.ConfigureServices` ：

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="health-checks"></a>健康狀態檢查

健康情況檢查可以用來做為具有端點路由的 *路由器* 。

新增 `MapHealthChecks` 以搭配端點路由使用健康情況檢查。 `MapHealthChecks`方法會接受類似的自 `UseHealthChecks` 變數。 使用 over 的優點 `MapHealthChecks` `UseHealthChecks` 是能夠套用授權，並對比對原則有更精細的控制。

在下列範例中， `MapHealthChecks` 會針對的健康狀態檢查端點呼叫 `/healthz` ：

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

<a name="hostb"></a>

## <a name="hostbuilder-replaces-webhostbuilder"></a>HostBuilder 取代 >webhostbuilder

ASP.NET Core 3.0 範本使用 [一般主機](xref:fundamentals/host/generic-host)。 舊版使用的 [Web 主機](xref:fundamentals/host/web-host)。 下列程式碼顯示 ASP.NET Core 3.0 範本產生的 `Program` 類別：

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

下列程式碼顯示 ASP.NET Core 2.2 範本產生的 `Program` 類別：

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 會保留在3.0 中，而且是在 `webBuilder` 上述程式碼範例中所見的型別。 <xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> 將在未來的版本中被取代，並取代為 `HostBuilder` 。

從到相依性 `WebHostBuilder` `HostBuilder` [插入 (DI) ](xref:fundamentals/dependency-injection)最重要的變更。 使用時 `HostBuilder` ，您只能將下列程式插入至 `Startup` 的函式：

* <xref:Microsoft.Extensions.Configuration.IConfiguration>
* <xref:Microsoft.Extensions.Hosting.IHostEnvironment>
* <xref:Microsoft.AspNetCore.Hosting.IWebHostEnvironment>

`HostBuilder`DI 條件約束：

* 讓 DI 容器只建立一次。
* 避免產生的物件存留期問題，例如解決多個 singleton 的實例。

如需詳細資訊，請參閱 [避免 ASP.NET Core 3 中的啟動服務插入](https://andrewlock.net/avoiding-startup-service-injection-in-asp-net-core-3/)。

## <a name="addauthorization-moved-to-a-different-assembly"></a>AddAuthorization 移至不同的元件

Microsoft.AspNetCore.Authorization.dll中的 ASP.NET Core 2.2 和較低的 `AddAuthorization` 方法： *Microsoft.AspNetCore.Authorization.dll*

* 已重新命名 `AddAuthorizationCore` 。
* 已移至 *Microsoft.AspNetCore.Authorization.Policy.dll*。

使用 *Microsoft.AspNetCore.Authorization.dll* 和 *Microsoft.AspNetCore.Authorization.Policy.dll* 的應用程式不會受到影響。

未使用 *Microsoft.AspNetCore.Authorization.Policy.dll* 的應用程式應該執行下列其中一項：

* 新增 *Microsoft.AspNetCore.Authorization.Policy.dll*的參考。 這種方法適用于大部分的應用程式，而且全部都是必要的。
* 切換至使用 `AddAuthorizationCore`

如需詳細資訊，請參閱) 多載中 [的重大變更 `AddAuthorization(o =>` 存在於不同的元件 #386](https://github.com/aspnet/Announcements/issues/386)。

## <a name="no-locidentity-ui"></a>Identity UI

Identity ASP.NET Core 3.0 的 UI 更新：

* 將套件參考新增至 [AspNetCore。 IdentityUI](https://nuget.org/packages/Microsoft.AspNetCore.Identity.UI)。
* 未使用頁面的應用程式 Razor 必須呼叫 `MapRazorPages` 。 請參閱本檔中的[ Razor 頁面](#razor-pages)。
* 啟動程式4是預設的 UI 架構。 設定 `IdentityUIFrameworkVersion` 專案屬性以變更預設值。 如需詳細資訊，請參閱 [此 GitHub 公告](https://github.com/aspnet/Announcements/issues/380)。

## SignalR

SignalRJavaScript 用戶端已從變更 `@aspnet/signalr` 為 `@microsoft/signalr` 。 若要回應這項變更，請變更檔案*package.json* 、 `require` 語句和 ECMAScript 語句package.js的參考 `import` 。

### <a name="systemtextjson-is-the-default-protocol"></a>System.Text.Json 是預設通訊協定

`System.Text.Json` 現在是用戶端和伺服器所使用的預設中樞通訊協定。

在中 `Startup.ConfigureServices` ，呼叫 `AddJsonProtocol` 以設定序列化程式選項。

**伺服器：**

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.PayloadSerializerOptions.WriteIndented = false;
        })
```

**用戶端：**

```csharp
new HubConnectionBuilder()
    .WithUrl("/chathub")
    .AddJsonProtocol(options =>
    {
        options.PayloadSerializerOptions.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a>切換至 Newtonsoft.Js開啟

如果您使用 [上的 Newtonsoft.Js的功能 System.Text.Js上不支援](/dotnet/standard/serialization/system-text-json-migrate-from-newtonsoft-how-to)，您可以切換回 `Newtonsoft.Json` 。 請參閱本文稍早 [ASP.NET Core 3.0 SignalR 專案中的使用 Newtonsoft.Js](#use-newtonsoftjson-in-an-aspnet-core-30-signalr-project) 。

## <a name="redis-distributed-caches"></a>Redis 分散式快取

ASP.NET Core 3.0 或更新版本的應用程式無法使用 [Redis](https://www.nuget.org/packages/Microsoft.Extensions.Caching.Redis) 套件。 將套件參考取代為 [StackExchangeRedis](https://www.nuget.org/packages/Microsoft.Extensions.Caching.StackExchangeRedis)。 如需詳細資訊，請參閱<xref:performance/caching/distributed>。

## <a name="opt-in-to-runtime-compilation"></a>加入執行時間編譯

在 ASP.NET Core 3.0 之前，views 的執行時間編譯是架構的隱含功能。 執行時間編譯會補充視圖的組建階段編譯。 它可讓架構在 Razor 修改檔案時，將視圖和頁面編譯 (的 *cshtml* 檔案) ，而不需要重建整個應用程式。 這項功能支援在 IDE 中進行快速編輯，以及重新整理瀏覽器以查看變更的案例。

在 ASP.NET Core 3.0 中，執行時間編譯是加入宣告的情節。 組建階段編譯是預設啟用的視圖編譯的唯一機制。 執行時間相依于 Visual Studio Code 中的 Visual Studio 或 [dotnet 監看](xref:tutorials/dotnet-watch) ，以在偵測到 *cshtml* 檔案的變更時重建專案。 在 Visual Studio 中，執行專案中的 *.cs*、 *cshtml*或 *Razor* 檔案的變更 (<kbd>Ctrl + F5</kbd>) ，但是未 (<kbd>f5</kbd>) 進行偵錯工具，則會觸發專案的重新編譯。

若要在您的 ASP.NET Core 3.0 專案中啟用執行時間編譯：

1. 請安裝 [AspNetCore Razor 。>microsoft.aspnetcore.mvc.razor.runtimecompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation) NuGet 套件。
1. 更新 `Startup.ConfigureServices` 以呼叫 `AddRazorRuntimeCompilation` ：

    針對 ASP.NET Core MVC，請使用下列程式碼：

    ```csharp
    services.AddControllersWithViews()
        .AddRazorRuntimeCompilation(...);
    ```
 
    針對 ASP.NET Core Razor 頁面，請使用下列程式碼：
 
    ```csharp
    services.AddRazorPages()
        .AddRazorRuntimeCompilation(...);
    ```
 
中的範例 https://github.com/aspnet/samples/tree/master/samples/aspnetcore/mvc/runtimecompilation 顯示在開發環境中有條件地啟用執行時間編譯的範例。

如需檔案編譯的詳細資訊 Razor ，請參閱 <xref:mvc/views/view-compilation> 。

## <a name="migrate-libraries-via-multi-targeting"></a>透過多目標來遷移程式庫

程式庫通常需要支援多個版本的 ASP.NET Core。 針對舊版 ASP.NET Core 編譯的大部分程式庫都應該繼續運作，而不會發生問題。 下列情況需要跨編譯應用程式：

* 程式庫依賴具有二進位 [中斷性變更](#breaking-api-changes)的功能。
* 程式庫想要利用 ASP.NET Core 3.0 中的新功能。 

例如：

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netcoreapp3.0;netstandard2.0</TargetFrameworks>
  </PropertyGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netcoreapp3.0'">
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="Microsoft.AspNetCore" Version="2.1.0" />
  </ItemGroup>
</Project>
```

用 `#ifdefs` 來啟用 ASP.NET Core 3.0 專用的 api：

```csharp
var webRootFileProvider =
#if NETCOREAPP3_0
    GetRequiredService<IWebHostEnvironment>().WebRootFileProvider;
#elif NETSTANDARD2_0
    GetRequiredService<IHostingEnvironment>().WebRootFileProvider;
#else
#error unknown target framework
#endif
```

如需在類別庫中使用 ASP.NET Core Api 的詳細資訊，請參閱 <xref:fundamentals/target-aspnetcore> 。

## <a name="miscellaneous-changes"></a>其他變更

.NET Core 3.0 和更新版本中的驗證系統會將不可為 null 的參數或系結屬性視為有 `[Required]` 屬性。 如需詳細資訊，請參閱 [[必要] 屬性](xref:mvc/models/validation?view=aspnetcore-3.1#required-attribute)。

### <a name="publish"></a>發佈

刪除專案目錄中的 *bin* 和 *obj* 資料夾。

## <a name="testserver"></a>TestServer

針對 <xref:Microsoft.AspNetCore.TestHost.TestServer> 直接搭配 [泛型主機](xref:fundamentals/host/web-host)使用的應用程式，請 `TestServer` 在中的上建立 <xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> <xref:Microsoft.Extensions.Hosting.GenericHostWebHostBuilderExtensions.ConfigureWebHost%2A> ：

```csharp
[Fact]
public async Task GenericCreateAndStartHost_GetTestServer()
{
    using var host = await new HostBuilder()
        .ConfigureWebHost(webBuilder =>
        {
            webBuilder
                .UseTestServer()
                .Configure(app => { });
        })
    .StartAsync();

    var response = await host.GetTestServer().CreateClient().GetAsync("/");

    Assert.Equal(HttpStatusCode.NotFound, response.StatusCode);
}
```

<a name="break"></a>

## <a name="breaking-api-changes"></a>中斷 API 變更

檢查重大變更：

* [ASP.NET Core 3.0 版中的重大變更完整清單](https://github.com/aspnet/Announcements/issues?page=1&q=is%3Aissue+is%3Aopen+label%3A%22Breaking+change%22+label%3A3.0.0)
* [中斷 Antiforgery、CORS、診斷、MVC 和路由的 API 變更](https://github.com/aspnet/Announcements/issues/387)。 這份清單包含相容性參數的重大變更。
* 如需 .NET Core、ASP.NET Core 和 Entity Framework Core 之間的2.2 對3.0 重大變更摘要，請參閱 [從2.2 版遷移至3.0 的重大變更](/dotnet/core/compatibility/2.2-3.0)。

## <a name="endpoint-routing-with-catch-all-parameter"></a>使用 catch-all 參數的端點路由

[!INCLUDE[](~/includes/catchall.md)]

## <a name="net-core-30-on-azure-app-service"></a>Azure App Service 上的 .NET Core 3。0

將 .NET Core 推出至 Azure App Service 的作業已完成。 .NET Core 3.0 適用于所有 Azure App Service 資料中心。